---
apiVersion: v1
kind: Service
metadata:
  namespace: home-assistant
  name: home-assistant
  labels:
    app: home-assistant
spec:
  type: ClusterIP
  selector:
    app: home-assistant
  ports:
  - port: 8123
    targetPort: http
    protocol: TCP
    name: http
